# In local dev, leave this unset — requests go through the Vite proxy to localhost:8080.
# In production (Vercel), set this to your deployed backend's API URL.
VITE_API_BASE_URL=https://velaris-perfume-store.onrender.com/api
